
{% macro link(routeName, label) %}
    <li class="nav-item{% if app.request.attributes.get('_route') == routeName %} active{% endif %}">
        <a class="nav-link" href="{{ path(routeName) }}">
            {{ label|trans }}
        </a>
    </li>
{% endmacro %}
